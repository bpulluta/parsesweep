# Command Reference

Every command is generated directly from the CLI itself, so the options and
their **defaults shown here always match runtime behavior**.

```{eval-rst}
.. click:: psweep.cli.app:_cli
   :prog: psweep
   :nested: full
```
