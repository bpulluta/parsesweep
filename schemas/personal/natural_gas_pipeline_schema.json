{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$metadata": {
    "domain": "Natural Gas Pipelines & Compressor Stations",
    "version": "3.0.0",
    "extraction": {
      "main_data_array": "requirements",
      "identifier_fields": [
        "jurisdiction.state",
        "jurisdiction.county"
      ],
      "context_objects": [
        "jurisdiction"
      ]
    },
    "compilation": {
      "deduplication": {
        "key_fields": ["feature", "specific_subject", "applies_to", "value_category", "value", "value_interpretation", "units", "obligation", "condition"],
        "ignore_fields": ["source_verbatim", "summary", "reasoning", "notes", "applicable_values", "range_low", "range_high"]
      },
      "value_typing": {
        "value_category_field": "value_category",
        "value_interpretation_field": "value_interpretation",
        "obligation_field": "obligation",
        "applicable_values_field": "applicable_values",
        "obligation_vocabulary": ["required", "prohibited", "conditional", "allowed", "recommended", "informational"],
        "value_category_vocabulary": ["quantitative", "qualitative"],
        "value_interpretation_vocabulary": ["exact", "minimum", "maximum", "range", "formula", "tiered", "enumerated"]
      }
    }
  },
  "title": "Natural Gas Pipeline & Compressor Station Ordinance Extraction Schema",
  "description": "Regulatory requirements for natural gas pipelines, compressor stations, and associated infrastructure.\n\nSCOPE: Natural gas compressor stations, natural gas pipelines (transmission and distribution), metering/regulator stations, oil and gas facilities, and associated equipment.\n\nEXCLUDE: Geothermal wells, wind turbines, solar installations, water/sewer utilities, electric substations, and telecommunications towers.\n\nWhen extracting from multi-use ordinances, ONLY extract provisions that explicitly apply to natural gas or oil and gas facilities.\n\nEvery requirement row is decomposed on four axes: WHAT is regulated (feature + specific_subject + requirement_description + applies_to), HOW MUCH (value + value_category + value_interpretation + units + range_low/range_high), HOW BINDING (obligation), and WHEN/WHERE (condition + applies_to). Evidence lives in source_verbatim, summary, reasoning, and section.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "jurisdiction",
    "requirements",
    "document_applicability"
  ],
  "properties": {
    "document_applicability": {
      "type": "object",
      "description": "Scope verification (extraction-quality field, excluded from compiled output).",
      "additionalProperties": false,
      "required": ["applies_to_natural_gas", "extraction_approach"],
      "properties": {
        "applies_to_natural_gas": {
          "type": "boolean",
          "description": "Does this document contain provisions regulating natural gas pipelines, compressor stations, or oil and gas facilities?"
        },
        "extraction_approach": {
          "type": "string",
          "description": "Which sections were extracted vs excluded. Format: 'Extracted [sections]; Excluded [sections]'"
        }
      }
    },
    "jurisdiction": {
      "type": "object",
      "description": "Geographic and temporal context. These fields become row-level context in compiled output.",
      "additionalProperties": false,
      "required": ["state", "county"],
      "properties": {
        "state": {
          "type": "string",
          "description": "US state name or abbreviation exactly as written in document."
        },
        "county": {
          "type": "string",
          "description": "County, city, town, or township name exactly as written."
        },
        "subdivision": {
          "type": ["string", "null"],
          "description": "Sub-jurisdiction name if applicable. Null if county-level."
        },
        "jurisdiction_type": {
          "type": ["string", "null"],
          "description": "Type of jurisdiction: County, City, Town, Township, Borough, or Village.",
          "enum": ["County", "City", "Town", "Township", "Borough", "Village", null]
        },
        "year": {
          "type": ["integer", "string", "null"],
          "description": "Year the ordinance was adopted or last amended. Null if not stated."
        },
        "source": {
          "type": ["string", "null"],
          "description": "URL or document reference for the source ordinance. Use the SOURCE_URL from the CONTEXT block at the top of the document if present."
        }
      }
    },
    "requirements": {
      "type": "array",
      "description": "Regulatory requirements extracted from the ordinance.\n\nEXTRACTION RULES:\n\n(1) ONE ROW PER REQUIREMENT: Each distinct regulatory provision gets its own row. When a single regulation applies to multiple subjects (e.g., 'setback from property lines and residential structures'), create SEPARATE rows and distinguish them with 'specific_subject'.\n\n(2) RANGES: When setbacks vary by zone (e.g., 100-500 ft depending on zone), put the range string in 'value', set value_interpretation='range', AND populate range_low/range_high.\n\n(3) TIERS: When a value depends on a band (capacity, distance, zone), emit ONE ROW PER TIER with value_interpretation='tiered' and the triggering band in 'condition'.\n\n(4) DAY/NIGHT: When noise limits differ by time, create separate rows with condition='daytime' or 'nighttime' and specific_subject='daytime limit'/'nighttime limit'.\n\n(5) FACILITY SCOPE vs TRIGGER: Put the governed facility type in 'applies_to' (e.g., 'Compressor station'); put the situational circumstance in 'condition' (e.g., 'nighttime'). Never mix the two.\n\n(6) PRIORITY FEATURES (extract if present):\n   - structures distance, property lines distance, residential zones distance\n   - front yard distance, roads distance, water bodies distance\n   - schools distance, hospitals distance\n   - distance between compressor stations\n   - noise (day/night separately)\n   - fencing, enclosure, minimum lot size\n   - permitted/conditional/special use districts\n   - required permits, bufferyard/landscaping",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["feature", "value_category", "obligation", "summary"],
        "properties": {
          "feature": {
            "type": "string",
            "description": "Standardized feature name (the WHAT axis). Use these exact values for cross-jurisdiction consistency.",
            "enum": [
              "structures distance",
              "front yard distance",
              "property lines distance",
              "residential zones distance",
              "water bodies distance",
              "roads distance",
              "schools distance",
              "hospitals distance",
              "community structures distance",
              "residential district boundary distance",
              "distance between compressor stations",
              "noise",
              "fencing",
              "minimum lot size",
              "enclosure"
            ]
          },
          "specific_subject": {
            "type": ["string", "null"],
            "description": "Sub-aspect of the feature that distinguishes this record from other rows sharing the same feature (WHAT axis). Participates in the dedup key. Use the ordinance's own framing. Null when the feature is already specific enough.",
            "examples": ["from property line", "from centerline", "daytime limit", "nighttime limit", "between compressor stations", null]
          },
          "applies_to": {
            "type": ["string", "null"],
            "description": "Facility type or scope the requirement governs (WHAT/WHERE axis), in the document's own terminology. Describes the kind of facility the rule binds. This is NOT the situational trigger (use 'condition') and NOT the obligation. Null when the requirement applies to all natural gas facilities generally.",
            "examples": ["Compressor station", "Transmission pipeline", "Gathering line", "Metering/regulator station", "All oil and gas facilities", null]
          },
          "requirement_description": {
            "type": ["string", "null"],
            "description": "Clean, obligation-free description of the regulated action or object (WHAT axis). NEVER include obligation language — no 'required', 'shall', 'may', 'must', 'prohibited'. Used for qualitative requirements; null for purely quantitative rows.",
            "examples": ["Sound barriers", "Exhaust muffler or exhaust box", "Complaint-response monitoring protocol", "Solid fencing", null]
          },
          "value_category": {
            "type": "string",
            "description": "REQUIRED. How this requirement's value should be interpreted (HOW MUCH axis). 'quantitative' = a measurable magnitude lives in 'value' (distance, dBA, acres, MW, hours, %, $). 'qualitative' = there is no magnitude; the substance lives in 'requirement_description' and 'value' should be null. Independent of 'obligation': a quantitative limit can be conditional and a qualitative item can be required.",
            "enum": ["quantitative", "qualitative"],
            "examples": ["quantitative", "qualitative"]
          },
          "value": {
            "type": ["number", "string", "null"],
            "description": "For quantitative requirements, the numeric magnitude ONLY (e.g., 500, 55, 8). Use a range string (e.g., '100-500') when the provision states a span, set value_interpretation='range', and populate range_low/range_high. For qualitative requirements set value to null and describe the requirement in requirement_description. NEVER embed units or obligation words ('required', 'may', 'shall') in value.",
            "examples": [500, 300, 60, 55, 8, 20, "100-500", null]
          },
          "value_interpretation": {
            "type": ["string", "null"],
            "description": "How the value is bounded or structured (HOW MUCH axis). 'exact' = single fixed value ('shall be 35 feet'); 'minimum' = floor ('at least', 'no less than'); 'maximum' = ceiling ('shall not exceed'); 'range' = a span — populate range_low and range_high; 'formula' = computed ('1.1x turbine height'); 'tiered' = value depends on a band — emit one row per tier; 'enumerated' = a discrete set of allowed values — pair with applicable_values. Null for qualitative requirements (present/absent).",
            "enum": ["exact", "minimum", "maximum", "range", "formula", "tiered", "enumerated", null],
            "examples": ["exact", "minimum", "maximum", "range", "formula", "tiered", "enumerated", null]
          },
          "units": {
            "type": ["string", "null"],
            "description": "Unit of measurement for a quantitative value. Null for qualitative requirements.",
            "examples": ["feet", "dBA", "dBC", "acres", "miles", "inches", null]
          },
          "range_low": {
            "type": ["number", "null"],
            "description": "Lower bound of the span when 'value' is a range (e.g., 100 for '100-500'). Domain-agnostic: works for distances, dBA, MW, acres, etc. Null unless value_interpretation='range'.",
            "examples": [100, 55, null]
          },
          "range_high": {
            "type": ["number", "null"],
            "description": "Upper bound of the span when 'value' is a range (e.g., 500 for '100-500'). Domain-agnostic: works for distances, dBA, MW, acres, etc. Null unless value_interpretation='range'.",
            "examples": [500, 65, null]
          },
          "obligation": {
            "type": "string",
            "description": "REQUIRED. How binding the requirement is (HOW BINDING axis), classified from the governing verb ONLY. Never place obligation language inside 'value' or 'requirement_description'.\n\n- 'required': mandatory — 'shall', 'must', 'is required', 'no less than'.\n- 'prohibited': forbidden — 'shall not', 'no person shall', 'may not', 'is not permitted', 'prohibited'.\n- 'conditional': discretionary authority — 'may require', 'at the discretion of', 'as a condition of approval', 'unless waived'.\n- 'allowed': explicitly permitted with no mandate — 'is permitted', 'may be located', 'is allowed'.\n- 'recommended': non-binding advisory — 'should', 'encouraged', 'best available technology'.\n- 'informational': definitions, procedures, measured baselines, or referenced external standards with no direct obligation.\n\nWhen multiple verbs appear, precedence: prohibited > required > conditional > recommended > allowed > informational. Cite the trigger word in 'reasoning'.",
            "enum": ["required", "prohibited", "conditional", "allowed", "recommended", "informational"],
            "examples": ["required", "prohibited", "conditional", "allowed", "recommended", "informational"]
          },
          "condition": {
            "type": ["string", "null"],
            "description": "Situational trigger or circumstance that governs WHEN or WHERE the requirement applies (WHEN/WHERE axis). This is the condition/circumstance, NOT the facility type (use 'applies_to') and NOT the obligation. Null if the requirement applies unconditionally.",
            "examples": ["nighttime", "daytime only", "after a signed complaint is received", "capacity exceeds 3 MW", "when screening is absent", null]
          },
          "applicable_values": {
            "type": ["array", "null"],
            "description": "Parallel interchangeable atomic values that share an IDENTICAL obligation, condition, and evidence. Use ONLY when (a) the values are categorical/enumerated (e.g., permitted district codes, acceptable permit types, equipment options) and (b) all share the same obligation + condition + source. Pair with value_interpretation='enumerated'. Null when a single value applies.",
            "items": { "type": "string" },
            "examples": [["AG", "RE", "SF 8.5", "R1", "R2", "R3"], ["Conditional use permit", "Minor use permit"], ["exhaust muffler", "exhaust box"], null]
          },
          "source_verbatim": {
            "type": ["string", "null"],
            "description": "Exact operative clause copied word-for-word from the ordinance (EVIDENCE axis). Never paraphrase — this is the audit anchor. If the passage exceeds 50 words, quote the operative head, insert '…', then the tail. Null only if no single passage can be quoted.",
            "examples": ["...shall not exceed a maximum of 55 dBa.", "...may require the temporary or permanent erection...of sound barriers...", null]
          },
          "summary": {
            "type": ["string", "null"],
            "description": "Human-readable summary of the requirement (EVIDENCE axis). May be paraphrased for clarity. Use 'source_verbatim' for the exact quote. Include enough context for verification."
          },
          "reasoning": {
            "type": ["string", "null"],
            "description": "Terse 'trigger → label' rationale (EVIDENCE axis), <=15 words, citing the governing verb and the value_category decision. Goes in ignore_fields and is hidden from compiled output. Null if self-evident.",
            "examples": ["'may require' → conditional; no magnitude → qualitative", "'shall not exceed' → required; number → quantitative/maximum", null]
          },
          "section": {
            "type": ["string", "null"],
            "description": "Specific ordinance section/article reference (EVIDENCE axis, e.g., 'Sec. 12-5-106', 'Article 5 § 552', '§ 270-4.10')."
          },
          "notes": {
            "type": ["string", "null"],
            "description": "Additional context not captured elsewhere: measurement conditions, exceptions, cross-references."
          }
        }
      }
    }
  }
}
